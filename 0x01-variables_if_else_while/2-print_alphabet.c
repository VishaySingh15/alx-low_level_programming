#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'This program outputs the alphabet in order'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	return (0);
}
